[pytest]
log_cli = true
log_cli_level = INFO
addopts = -v -s
testpaths = tests
markers =
    integration: Integration tests
    customer_registration: Tests related to customer registration
    menu: Tests related to the menu feature
    address_eligibility: Tests related to address eligibility
    wip: Work in progress tests
    wip1: Work in progress test 1